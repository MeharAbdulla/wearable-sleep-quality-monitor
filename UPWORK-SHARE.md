# Upwork-safe sharing

This is a personal portfolio prototype (build window noted in the README).

- Do not commit `secrets.h` or real `.env` files.
- Copy `secrets.example.h` or `.env.example` locally for Wi-Fi / MQTT / API values.
- No client-branded or NDA work is included.
